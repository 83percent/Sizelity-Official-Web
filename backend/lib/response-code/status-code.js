// 200
const success = 200;

// 400
const badRequest = 400;
const unauthorized = 401;
const noData = 404;
const invalid = 412;
const exist = 419;

// 500
const error = 500;
module.exports = {
    // 200
    success,

    //400
    badRequest,
    unauthorized,
    noData,
    invalid,
    exist, 

    // 500
    error
};